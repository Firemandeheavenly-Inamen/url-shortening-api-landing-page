import React from "react";
import './form.css'

const Form = () => {
  return (
    <input
      type="text"
      id="url"
      placeholder="Shorten a link here..."
    />
  );
};

export default Form;
